name: checkMyrole
on: [push]

jobs:
  checkDebian:
    runs-on: [ubuntu-18.04]
    steps:
      - name: checkout
        uses: actions/checkout@v2.0.0
      - name: Run some commands
        run: |
          ls
          pwd
        
