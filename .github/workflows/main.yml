name: CI
on: push

jobs:
  molecule:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [centos, ubuntu-latest]
        include:
        - os: centos7
          env : MOLECULE_DISTRO=centos7
        - os: ubuntu-latest
          env : MOLECULE_DISTRO=ubuntu
          
            
    steps:
      - uses: actions/checkout@v2
        with:
          path: "${{ github.repository }}"
      - name: molecule
        uses: robertdebock/molecule-action@1.1.1
        with:
          image: ${{ matrix.os}}
          options: "--parallel all"
