name: CI
on: push

jobs:
  molecule:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        scenario:
            - centos-7
            - debian-9
            
    steps:
      - uses: actions/checkout@v2
        with:
          path: "${{ github.repository }}"
      - uses: gofrolist/molecule-action@master
        

